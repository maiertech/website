import type { NoteType } from '@maiertech/sveltekit-helpers';

const meta: NoteType = {
	title: 'Environment variables in VS Code',
	description:
		'Instead of setting environment variables in your terminal, you can also set them in your user settings.json in VS Code.',
	publishedDate: '2025-03-06',
	path: '/notes/env-vars-in-vscode'
};

export default meta;
